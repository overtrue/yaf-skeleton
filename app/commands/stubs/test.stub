<?php

/**
 * Test for Controller: {{controller}}.
 *
 * @author {{username}} <{{email}}>
 */
class {{controller}}Test extends TestCase
{
    public function testHandle()
    {
        $controller = mock_controller({{controller}}::class);

        // 设置期望的异常
        // $this->shouldAbort('参数错误');

        // 调用并获取返回值
        // $response = $controller->handle();
        // $this->assertSame('It works!', $response);

        // 完成后删除这行
        $this->markTestIncomplete();
    }
}
